{
  "name": "angular-assignment",
  "private": true,
  "version": "0.0.0",
  "description": "RentoMojo angularjs assignment",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.9.0",
    "bower": "^1.7.7"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "update-index-async": "node -e \"var fs=require('fs'),indexFile='index-async.html';\""
  },
  "dependencies": {
    "npm-git-install": "^0.3.0"
  }
}
